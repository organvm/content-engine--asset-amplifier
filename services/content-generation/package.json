{
  "name": "@cronus/content-generation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "*",
    "@cronus/config": "workspace:^",
    "@cronus/db": "workspace:*",
    "@cronus/domain": "workspace:*",
    "@cronus/logger": "workspace:*",
    "@cronus/storage": "workspace:*",
    "blockhash-core": "*",
    "fluent-ffmpeg": "*",
    "sharp": "*"
  },
  "devDependencies": {
    "@types/fluent-ffmpeg": "^2.1.28",
    "@types/node": "^26.1.1"
  }
}
