{
  "name": "@cronus/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cronus/config": "workspace:*",
    "@cronus/db": "workspace:*",
    "@cronus/domain": "workspace:*",
    "axios": "^1.18.1",
    "commander": "*",
    "ora": "*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1"
  }
}
